# Project Scope and Status

This file tracks project phase and status. For the detailed launch scope, see `CLAUDE.md` Section 1. That table is the single source of truth for launch scope and is not duplicated here to avoid drift.

---

## Current phase

Site is live. Work is implementation and iteration, not strategic redesign.

The canonical production domain is:

`https://christopherrobindesign.com`

Use this domain for canonical tags, Open Graph URLs, sitemap entries, robots references, public links, project documentation and implementation briefs unless Christopher explicitly changes the decision.

`www.christopherrobindesign.com` and `christopherrobin.design` may remain available as secondary domains or redirect sources, but they are not the canonical public domain.

---

## Status log

- **2 July 2026** — File created. Site confirmed live on Cloudflare (auto-deploy from `main`). Domain nameserver repoint to Cloudflare still pending but explicitly non-blocking (see `CLAUDE.md` Section 1). Four known issues open — see `docs/known-issues.md`.
- **3 July 2026 (evening)** — Landing motion & layout pass (`feat/landing-motion-pass`): hero illustration replaced with inline SVG (re-vectorised from the approved raster via Recraft — the Drive trace was unusable; see `scripts/prepare-hero-svg.ts`), GSAP bottom-up reveal + idle float + scroll parallax + cursor tilt, Lenis smooth scroll site-wide sharing a scroll source with ScrollTrigger, hero scaled to the `website_mockup.png` proportions, card row rhythm adjusted. All motion respects `prefers-reduced-motion`. Referenced `crd-hero-laptop-microanimation-brief.md` was not found in Drive or repo — worked to the summary in the page brief. Three.js layer deliberately omitted. `ProjectDetail.tsx` untouched (nav-overlap bug still open, out of scope). Follow-up same evening per Christopher: all white/paper fills removed from the hero SVG (converted to mask knockouts — real transparency, texture shows through), and the vine reveal now grows radially out of the stem/keyboard junction. Second follow-up: the SVG pipeline was **retired entirely** — Christopher wants the mockup's sketch fidelity exactly, and every vectorisation coarsened it. The hero is the original raster export again (cropped to content, `web_hero_laptop_cropped.*`), with the growth reveal done as a radial mask from the keyboard junction instead of per-path stagger. Idle float, parallax and cursor tilt unchanged.
- **4 July 2026 (night)** — Checkers hero image was cropped on both left/right edges in the `/work` grid, cutting into price badges and product shots. Cause: the card forced every project's cover into a fixed `aspect-[3/4]` box regardless of the actual asset shape — Checkers' retail leaflet is 1327×1536 (0.864 ratio, noticeably wider than the 0.75 box), where the magazine covers (Kruger 0.764, Playboy 0.707) happen to sit close enough to 3:4 that the crop wasn't visible. Fixed by deriving the container's `aspect-ratio` from the cover's own `img.w`/`img.h` at render time (`Work.tsx`) instead of a fixed class — no lookup table to maintain, self-corrects for any future project regardless of its native shape. Verified all five current covers render uncropped.
- **4 July 2026 (evening)** — Four fixes per Christopher: (1) **CRD project removed** from the `/work` grid and its detail route — it had no Drive asset by design and only ever showed a repeated placeholder image (`futures.jpg`); removed the project entry, `ProjectDetail.tsx`'s now-unreachable legacy no-manifest gallery branch (`embla-carousel-react` usage included), and the dead asset file. **This is a launch-scope change** — `CLAUDE.md` Section 1's project table and scope-history note updated to five projects, flagging the removal so the doc doesn't drift stale. (2) **Every project now gets a layout/feature caption**, not just multi-group issues — `ProjectContentFlow`'s `showLabels` gate (previously `> 1 distinct group`) removed; single-group manifests whose generated key is a meaningless placeholder (`_root`) fall back to the project title instead of showing "Root". Verified: Uncut shows "Issue 93", Leisure Boating shows "Leisure Boating Magazine" (its group key is literally `_root`), Playboy's per-feature captions unchanged. (3) **Slight drop shadow** added consistently to issue covers and the framed spread/page cards (a warm forest-tinted shadow, not a harsh black one) — previously the cover had a strong `shadow-xl` and the card wrapper had almost none (`shadow-sm`); now both use the same soft elevation. (4) **Work-grid thumbnails reduced** — cover image column narrowed from `md:w-3/5` to `md:w-2/5` (with a `max-w-sm` cap) and the text column widened to compensate; this is the `/work` overview grid (`Work.tsx`) only, per Christopher's "not each project" — `ProjectDetail.tsx`'s own per-project hero is untouched.
- **4 July 2026 (later)** — Mockup-exact pass per Christopher: **palette re-tuned to `website_mockup.png`** (sampled programmatically, not eyeballed) — forest ink deepened `#12322F`→`#0E2320`, gold deepened `#C7A35A`→`#A57C33`, parchment warmed `#F2EDE0`→`#F4ECD9`, semantic tokens matched. This intentionally deviates from Brand Bible v0.6 hexes on Christopher's explicit instruction ("colour must match exactly"); the Bible needs a v0.7 update to stay the source of truth. Nav-hub cards rebuilt to mockup proportions (taller, text-4xl serif titles, bottom-pinned arrows). Animated edge foliage added: green watercolour sprigs cut from the mockup by `scripts/extract-foliage.ts` (olive keying — the sprigs are NOT green-dominant in RGB), grow in from the left edge on load and hold a barely-there sway (SiteBackground, reduced-motion static). Project pages: issue covers now render whole at natural aspect with shadow (previously object-cover-cropped to strips); every spread/page/cover reveals on scroll via a quiet rise-and-settle (framer `whileInView`, honours reduced motion). Mockup's stale PROCESS nav item ignored as before.
- **4 July 2026** — Project detail pages (`feat/landing-motion-pass`): `ProjectContentFlow` now groups consecutive spread/page blocks by the manifest's `group` field into one contained, framed card (`max-w-5xl`, hairline dividers, no gap between pages of the same feature) instead of edge-to-edge full-width sections — this is what "reduce spread sizes" and "must look like they belong to the same article" meant in practice: width containment plus shared framing per editorial feature. Multi-feature projects (Playboy, Checkers) get an auto-derived caption between features (e.g. "BADASS ERIC CHURCH"); single-feature projects (Uncut, Kruger, Leisure Boating) don't, since there's nothing to distinguish. `DoubleSpread` gained a subtle centre gutter shadow (sells the two exports as one open spread) and per-half hover/click affordance; every page and spread half now opens a new `ProjectLightbox` (Radix Dialog, keyboard + click nav) scoped to its own group only — browsing never crosses into a different feature or issue. Cover blocks (issue-boundary reveals) are untouched, still full-bleed. Verified against Kruger (single group), Playboy (21 groups incl. a lone mid-sequence cover), and Leisure Boating (page-only, no spreads) at desktop and mobile widths. `ProjectDetail.tsx`'s nav/hero remained untouched (nav-overlap bug still open, out of scope, per Section 3). Leisure Boating's known 3D-mockup dead space is unchanged — pre-existing, not a regression.
- **3 July 2026** — Content-flow system built (manifest generator, `ContentBlock`/`ProjectContent` types, `DoubleSpread`, `ProjectContentFlow`) and all five non-CRD launch projects migrated to it; see `docs/adding-a-project.md`. Fixed three of the four known issues as verified side effects of using correct manifest data (Playboy's wrong hero, Kruger's merged-spread/missing-`DoubleSpread` problem, Kruger's cover/issue mismatch) — see `docs/known-issues.md`. Nav-bar/hero-overlap bug and Leisure Boating's 3D mockup cover remain open. CRD still has no Drive asset by design and bypasses the content-flow system entirely.
- **9 July 2026** — Contact page reworked and live: a mailto-based enquiry form (no backend, nothing stored on the site — see `src/pages/Contact.tsx`). Playboy SA hero-image issue confirmed resolved (re-verified against `src/data/projects.ts` and the current asset path; see `docs/known-issues.md`). Landing-page workflow-diagram redesign and copy refinements shipped: `ConnectedSystemsDiagram` (the "How the Work Moves" section) rebuilt from boxed nodes/straight connectors to hand-drawn typographic labels and organic bezier connectors, with a simplified typographic-list treatment below `md`; hero subheadline, Selected Work intro line and footer credit copy updated; vertical rhythm between homepage sections checked live and confirmed already consistent (`py-24 md:py-36` throughout), no change needed. Nav-bar overlap bug remained quarantined and untouched throughout this workstream.
- **10 July 2026** — `https://www.christopherrobindesign.com` confirmed working again and set as the canonical production domain. Canonical-domain uncertainty is no longer an open Phase 0 decision. All docs, metadata and future briefs should use the `www` `.com` domain as primary.
- **19 July 2026** — Canonical production domain changed to `https://christopherrobindesign.com`. Cloudflare now serves apex `.com` directly through the `crd-portfolio` Worker route, redirects `www.christopherrobindesign.com` to apex `.com`, and redirects `christopherrobin.design` / `www.christopherrobin.design` to the matching apex `.com` path with query strings preserved.
- **17 July 2026** — Production-refinement pass (`feat/production-refinement`), full-site brief. (1) Every route (incl. all ten `/project/*` pages) now prerenders its own static HTML with unique title, description, canonical and OG/Twitter tags from a single meta source (`src/lib/meta.ts`); sitemap generated at build from the same route list. (2) Mobile menu triggers are `type="button"` with Escape-to-close and focus management. (3) One descriptive `h1` per page; duplicated project-name `h2` in the detail rail demoted to a paragraph. (4) Homepage hero fits a 1280×800 fold (illustration width-capped, spacing tightened, italic emphasis replaced with moss roman). (5) Decorative eyebrow labels retired; ordinal/contextual labels kept. (6) Motion restrained to one coordinated entrance per page; underline sweep now transform-based. (7) Work-page scroll/state restoration: browser Back, Back to Work and reload all reopen `/work` at the exact position with focus returned to the launching card. (8) Project pages gained prev/next navigation, a variant-matched enquiry CTA and layout variants (editorial / identity plates / campaign). (9) Responsive hardening verified at 320/375/414/768/1280 — no horizontal overflow. (10) Spread-ratio warning removed; LCP assets load eagerly. (11) Repo cleanup: shadcn ui kit, dead hooks, Replit plugins, superseded hero rasters and 48 unused dependencies removed — see `docs/cleanup-inventory-2026-07-17.md`. Nav/hero overlap on project pages re-verified as fixed and moved to Resolved in `known-issues.md`. On-brand 404 replaced the template one.

**Still open, not yet resolved:**

- Homepage "Selected Work" currently features three projects (Kruger, Behelm, Boutique Essentials). The selection hasn't been strategically revisited; don't read the current set as a deliberate final decision.
- Leisure Boating still needs a proper flat cover asset when available.
- Mailto contact form works as a temporary implementation but is not a robust backend submission system.
- Offline repository cleanup before GitHub publishing removed the 143 MB `new images` staging drop, root Replit metadata, `.agents/`, `.claude/`, `archive/` and `attached_assets/`. Remaining owner-review items in `docs/cleanup-inventory-2026-07-17.md`: unreferenced source-artwork PNGs and `public/opengraph.jpg`.
